[pytest]
markers =
    slow: marks tests as slow (deselect with '-m "not slow"')
    serial

filterwarnings =
    ignore::DeprecationWarning:redis.*
    ignore::DeprecationWarning:pyramid_redis_sessions.*

